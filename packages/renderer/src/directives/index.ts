// import { App } from 'vue'
// import { hyphenate } from '/@/utils/string'
// import { WatchScroll } from './watchScroll'

// const directives = { WatchScroll }

// export default function(app: App) {
//   Object.keys(directives).forEach(key => {
//     app.directive(hyphenate(key), directives[key as keyof typeof directives])
//   })
// }

export { WatchScroll } from './watchScroll'
